# Future scope

- Chrome Extension: call the existing `POST /api/chat` API with a selected text context. It is not part of the current exhibition delivery.
- Enterprise Agent management: add only after the exhibition Demo is accepted. Do not add an administration backend to the current project.
- Real medicine data: replace `backend/data/medicines.json`, preserve source URLs and collection dates, then run the import command.
